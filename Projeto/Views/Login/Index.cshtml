@model Projeto.Models.Usuario
@{
    ViewData["Title"] = "Login";
}

@section css {
    <link rel="stylesheet" href="~/css/headless.css">

    <link rel="stylesheet" href="~/css/login.css">
}
<!-- 

 -->
<div class="split left">

</div>

<div class="split right">
 
   <h1 class="text-center mt-3">Login</h1>
        <div class="container col-11 col-lg-6">
            <form class="d-flex flex-column gap-4" asp-action="Index">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <input asp-for="Login" class="form-control bordas"  placeholder="Digite seu usuário"/>
                    <span asp-validation-for="Login" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="password" asp-for="Senha" class="form-control bordas" placeholder="Digite sua senha" />
                    <span asp-validation-for="Senha" class="text-danger"></span>
                </div>
                @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
                {
                    <p class="text-danger">@ViewBag.ErrorMessage</p>
                }
                <div class="form-group text-center mt-3">
                    <input type="submit" value="Entrar" class="btn btn-danger log" />
                </div>

            <div class="text-center">
                <a class="text-decoration-none fw-bold" asp-area="" asp-controller="Home" asp-action="Index"><i class="bi bi-arrow-left"></i> Voltar</a>
            </div>
                
            </form>

</div>
